Battery Model_v1 - 30/05/2021

1)Open workspace.mat and load variables
2)Open WholeAssembly.slx(SingleCell.slc for single cell) and calparam.slx (calacparam_1.slx for single cell)
3)Run calcparam.slx(calacparam_1.slx for single cell) to calculate the parameters R0,R1,C1
4)Run WholeAssembly.slx (SingleCell.slc for single cell)